#include <stdio.h>
int add(int a, int b);
int main(int x, int y)
{
    int x,y;
    printf("Enter the values of x and y: ")
    scanf("%d, %d",&x,&y)
    printf("Sum of %d and %d is: %d",x,y,add(x,y))
    return 0;
}
int add(int a, int b)
{
    int s=x+y
    return s;
}